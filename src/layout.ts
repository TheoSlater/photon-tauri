/**
 * Frontend coordinates for the first Photon browser shell.
 *
 * Rust mirrors these values in browser/layout.rs because the native Wry page
 * is positioned independently from the React document.
 */
export const TOP_BAR_HEIGHT = 40;
export const PAGE_PADDING = 4;
